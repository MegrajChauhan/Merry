;; Until we put masm into bin, we need to use the path from build

$define _AVAI_DIV_
$include ../example/lib/math.masm
$include ../example/lib/print.masm

.text:
proc main


main:
  mov Ma, 1
  mov Mb, 2
  call addX
  call print
  hlt
