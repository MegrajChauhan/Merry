use std::fs::File; // for file IO
use std::collections::String; // strings
use std::collections::Vector; // vectors for all arguments

// we expect the ending extension for the input file to be ".merry"

enum Args_t
{
  _FILE_NAME, // the file provided[must follow 
}

// represents the command line options
struct CommandLine
{
  _args_: Vec<String>,
  // now for each option we expect
}
